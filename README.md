# Deep-Claw-AI-Agent
Deep Claw is an event-sourced, self-narrating trading organism: price ticks become episodes, episodes accumulate into a chain (memory), the chain is read by both a learned numeric model (confidence/sizing) and a language model (Claude, narrative judgment)
